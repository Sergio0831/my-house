const Header = {
  render: () => {
    return `
    <header class="hero">
      <div class="banner">
        <h1 class="banner__title">furniture collection</h1>
        <a href="#" class="banner__btn btn">
          shop now
        </a>
      </div>
    </header>;
    `;
  },
};

export default Header;
